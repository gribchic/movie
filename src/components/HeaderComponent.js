import React from 'react';

const Header = () => {
    return (
        <nav className="navbar navbar navbar-dark bg-dark">
            <div className="container-fluid">
                <span className="navbar-brand text-uppercase" >Filmer</span>
            </div>
        </nav>
    );
}

export default Header;
